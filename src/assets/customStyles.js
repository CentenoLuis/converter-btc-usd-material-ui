import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  root: {
    width: 300,
    // margin: "auto",
    // objectFit: "contain",
  },
  media: {
    // height: 200,
    // blockSize: 300,
    height: 200,
    width: "100%",
    objectFit: "contain",
    marginTop: "2",
  },
});

export default useStyles;
